import {
} from './actions'

const initialState = {
  list: [],
}

function candidatesReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default candidatesReducer
